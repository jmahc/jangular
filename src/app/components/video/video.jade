section#video
  .homepage-hero-module
    .video-container
      .filter.no-stretch
      video.video-js(autoplay='', loop='', ng-repeat="videoItem in vm.videoItems", ng-init="refresh()")
        source(src='{{ videoItem.file }}', type='{{ videoItem.type }}')
        | Your browser does not support the video tag. I suggest you upgrade your browser.
      .poster
        img.no-stretch(src='assets/img/location.jpg', alt='')
